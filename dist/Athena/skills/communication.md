# Communication skill

Email flow:

```text
draft_email → review → send_email (confirmation)
```

Inbox:

```text
search_inbox → read_email
```

Never send silently. Do not expose passwords or full private threads into logs.
