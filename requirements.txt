# Athena Offline AI Assistant - dependencies
# Python 3.12

# --- Core / config ---
requests>=2.32
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
pyyaml>=6.0
psutil>=5.9
pytest>=8.0

# --- Voice: wake word, STT, TTS ---
openwakeword==0.6.0
faster-whisper==1.2.1
nvidia-cublas-cu12>=12.9
nvidia-cudnn-cu12>=9.24
piper-tts==1.4.2
sounddevice==0.5.5
scipy==1.17.1
numpy==2.5.1

# --- Brain: local LLM + embeddings (via Ollama) ---
ollama==0.6.2

# --- RAG: document ingestion + vector search ---
pypdf==6.14.2
python-docx==1.2.0

# --- Vision: screenshot, OCR, UI automation ---
pillow==12.3.0
rapidocr-onnxruntime==1.4.4
onnxruntime==1.26.0
pyautogui==0.9.54

# --- GUI: system tray + Owl's Vigil HUD ---
pystray==0.19.5
# tkinter ships with Python (realtime dashboard)

# ----------------------------------------------------------
# External requirements (not pip-installable):
#   1. Ollama running locally         -> https://ollama.com
#   2. Chat/plan model:  ollama pull qwen3.5:9b
#   3. Embedding model:  ollama pull nomic-embed-text
#   4. Piper voice model in voice/models/
#        en_US-lessac-medium.onnx (+ .onnx.json)
#   5. OpenClaw (optional execution layer)
# ----------------------------------------------------------
