# Data engineering skill

Prefer stack tools over invented shell:

- `check_data_stack`, `check_docker`, `check_clickhouse`, `check_airflow`, `check_mysql`
- `clickhouse_query` is read-only (mutating SQL blocked)
- `analyze_csv` / `analyze_excel` for deterministic profiles — LLM interprets, does not invent numbers
