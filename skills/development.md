# Development skill

Use `tools/development`:

- Inspect with `read_project`, `git_status`, `git_diff`, `git_log`
- Change with `write_code` then `run_tests`
- Commit locally only (`git_commit`) — never push by default
