# Windows skill

Prefer deterministic application tools:

- `open_application` / `close_application` by friendly name
- `get_system_status` / `list_processes`
- Never invent executable paths — discovery + matcher resolve them

Confirm before shutdown/restart/sleep.
